## 1. Threat Analysis
On June 11, 2026, at approximately 09:30:05, a malicious PowerShell script was executed on host `WIN-FIN-07` (IP address `10.1.1.20`) by user `ACME\jsmith` (as seen in `sysmon.log` and `windows_event.log`). The script was downloaded from `45.137.21.130:80` (as indicated by `firewall.log` and `suricata.log`), which is likely a command and control (C2) server. The script then established a connection to the C2 server on port `8080` (as seen in `firewall.log` and `suricata.log`). Subsequently, a new user account `svc_backup` was created with administrative privileges (as seen in `windows_event.log`), and the account was used to access a file share on `WIN-FILE-01` (as seen in `windows_event.log`). The attack chain involves the initial compromise of `WIN-FIN-07`, the creation of a new user account, and the access to sensitive files on `WIN-FILE-01`.

## 2. MITRE ATT&CK Mapping
The following techniques were observed:
* T1059 (Command and Scripting Interpreter) - PowerShell was used to execute malicious scripts.
* T1190 (Exploit Public-Facing Application) - The initial exploitation of the vulnerability is not explicitly shown in the logs, but the download of a malicious script from a C2 server suggests that a vulnerability was exploited.
* T1078 (Valid Accounts) - A new user account `svc_backup` was created with administrative privileges.
* T1133 (External Remote Services) - The malicious script communicated with a C2 server on `45.137.21.130:8080`.
* T1210 (Exploitation of Remote Services) - The attack involves the exploitation of remote services, including the creation of a new user account and access to file shares.

## 3. Severity
**High** — The creation of a new user account with administrative privileges and the access to sensitive files on a file share indicate a high level of severity, as the attackers have gained significant control over the system and potentially have access to sensitive data.

## 4. Investigation Plan
1. Confirm the initial exploitation vector by analyzing logs from `WIN-FIN-07` and other systems on the network.
2. Investigate the `svc_backup` account and its activities, including any further access to sensitive files or systems.
3. Analyze network traffic to and from `45.137.21.130:8080` to determine the extent of the C2 communication.
4. Identify any other systems that may have been compromised by the attackers.

## 5. Response Plan
Based on the provided runbook, the following steps should be taken:
### Containment
1. Isolate `WIN-FIN-07` and `WIN-FILE-01` from the network to prevent further compromise.
2. Block traffic to and from `45.137.21.130:8080` at the firewall.
3. Disable the `svc_backup` account and revoke any active sessions or tokens.
### Eradication
1. Remove any malware or backdoors from `WIN-FIN-07` and `WIN-FILE-01`.
2. Patch or close any vulnerabilities that may have been exploited.
3. Hunt for any remaining footholds across the estate using IOCs from analysis.
### Recovery
1. Restore systems from known-good backups after confirming backups are clean.
2. Rebuild from golden images when restore integrity is uncertain.
3. Re-enable access gradually and monitor for reinfection or renewed beaconing.

## 6. Uncertainties & Flags
* **FLAG:** The initial exploitation vector is not explicitly shown in the logs, and further investigation is needed to determine how the attackers gained initial access.
* **FLAG:** The extent of the compromise is not fully understood, and further analysis is needed to determine if other systems were compromised.
* **FLAG:** The attackers' intentions and goals are not clear, and further investigation is needed to determine the motivations behind the attack.
* **FLAG:** The `svc_backup` account was created with administrative privileges, but it is not clear if this account was used for any other malicious activities beyond accessing the file share on `WIN-FILE-01`.